import { Component } from "react";

class Console extends Component {
    constructor(props) {
        super(props)
    }
    render() {
        return (
        <>
            {this.props}
        </>
        )
    }
}

export default Console;